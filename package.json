{
  "name": "nsr",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "clean": "rimraf node_modules .turbo packages/*/node_modules apps/*/node_modules config/*/node_modules  pnpm-lock.yaml packages/*/dist apps/*/dist packages/*/.turbo"
  },
  "devDependencies": {
    "rimraf": "6.0.1",
    "turbo": "2.1.2"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=18"
  }
}
